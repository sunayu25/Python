Ex.1 學生成績與課程歸類統計

題目描述：
系統接收到一組未整理的學員選課與成績資料（格式為包含字典的清單）。請寫一個程式將資料重新整理為以課程為 Key 的字典，並計算每門課程的平均分數以及最高分的學生名稱。

資料結構範例：

records = [
    {"name": "Alice", "course": "Python", "score": 85},
    {"name": "Bob", "course": "Web", "score": 92},
    {"name": "Charlie", "course": "Python", "score": 95},
    {"name": "David", "course": "Web", "score": 78},
    {"name": "Eve", "course": "Python", "score": 90},
]

要求：

建立一個新字典，結構為：
{ 課程名稱: {"students": [學生清單], "avg_score": 平均分, "top_student": 最高分學生名稱} }

平均分數請四捨五入至小數點後第一位。

預期輸出：

【課程統計結果】
Python:
  - 學生列表: ['Alice', 'Charlie', 'Eve']
  - 平均分數: 90.0
  - 最高分學生: Charlie

Web:
  - 學生列表: ['Bob', 'David']
  - 平均分數: 85.0
  - 最高分學生: Bob

  ----


Ex.2 倉庫庫存扣抵與補貨警告

題目描述：
某電商倉庫目前有初始庫存字典，今天有一批訂單（出貨清單）以及一批進貨（補貨清單）。請寫一個程式更新最終庫存，並找出庫存數量低於安全水位（預設為 5 件）的商品。

輸入資料：

```python
inventory = {"Apple": 15, "Banana": 8, "Orange": 3, "Milk": 10}
orders = {"Apple": 5, "Banana": 6, "Orange": 4, "Bread": 2}  # 出貨（Bread 無庫存）
restock = {"Orange": 10, "Milk": 2, "Bread": 10}  # 進貨
```

要求：

扣除 orders 中的數量。若庫存不足以扣抵（如 Orange 原本 3 件卻要出貨 4 件，或 Bread 原本 0 件），請記錄該筆訂單扣抵失敗，庫存扣至 0 為止（不為負數）。

加入 restock 的進貨數量。

輸出最終庫存字典，以及警示哪些商品的最終庫存小於 5 個。

預期輸出：

【出貨異常警示】
- Orange 庫存不足！欲扣除 4，實際僅扣除 3，尚缺 1
- Bread 無庫存記錄，無法出貨！

【最終庫存明細】
{'Apple': 10, 'Banana': 2, 'Orange': 13, 'Milk': 12, 'Bread': 10}

【低庫存警示 (少於 5 件)】
- Banana (剩餘 2 件)


----

Ex.3 文章單字頻率分析與多重過濾

題目描述：
給定一段包含標點符號的英文文章，請寫一個程式統計每個單字出現的次數。必須先將文章轉為小寫並去除標點符號，同時過濾掉常見的停用詞（Stop Words，如 the, a, is, and 等無實質意義的詞）。最後依出現次數由高到低排序，印出前 N 名。

輸入資料：

```python
text = """
Python is an easy to learn, powerful programming language. 
Python has efficient high-level data structures and a simple but effective approach to object-oriented programming.
"""
stop_words = {"is", "an", "to", "has", "and", "a", "but"}
```

要求：清理字串：移除數字與標點符號 , 和 .，並統一轉為小寫。

使用字典記錄每個有效單字的出現次數。

過濾掉 stop_words 集合中的單字。

將結果依據出現次數由大到小排序（次數相同時按字典序排序），並印出出現次數 $\ge 2$ 的單字字典。

預期輸出：

【單字頻率統計 (排除停用詞結果)】
{'python': 2, 'programming': 2}

---

Ex.4 API 多重條件資料篩選與轉換

題目描述：
你從伺服器 API 取得了一組使用者帳號資料庫（字典格式，Key 為使用者 ID，Value 為使用者詳細資訊的字典）。請使用 Python 的字典推導式（Dictionary Comprehension）對這筆資料進行雙重篩選與格式轉置。

輸入資料：

users = {
    "user_01": {
        "name": "Alex",
        "age": 25,
        "active": True,
        "roles": ["admin", "editor"],
    },
    "user_02": {"name": "Bella", "age": 17, "active": True, "roles": ["user"]},
    "user_03": {
        "name": "Chris",
        "age": 30,
        "active": False,
        "roles": ["editor"],
    },
    "user_04": {"name": "Daisy", "age": 22, "active": True, "roles": ["user"]},
    "user_05": {
        "name": "Eric",
        "age": 28,
        "active": True,
        "roles": ["admin", "user"],
    },
}

要求：
請寫出字典推導式，過濾出滿足以下所有條件的使用者：

active 狀態必須為 True。

age 必須大於等於 18 歲。

輸出格式改變為：{ "USER_ID": "姓名 (角色1, 角色2)" }（ID 改為大寫，角色以逗號分隔）。

預期輸出：

{
    'USER_01': 'Alex (admin, editor)',
    'USER_04': 'Daisy (user)',
    'USER_05': 'Eric (admin, user)',
}

