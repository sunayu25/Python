Ex.1 英雄角色狀態管理

題目描述：

設計一個 Hero 類別，用來管理遊戲角色的狀態。

要求：

初始化函式 __init__(self, name, hp, attack_power)：

name：英雄名稱

hp：生命值（整數）

attack_power：攻擊力（整數）

方法 attack(self, target)：

扣除目標 target 的 hp（扣除量等於自身的 attack_power）。

印出攻擊訊息：「{攻擊者} 攻擊了 {目標}，造成 {攻擊力} 點傷害！」。

若目標的 hp 小於等於 0，將其 hp 設為 0 並印出「{目標} 已經陣亡！」。

方法 heal(self, amount)：

增加自身 hp，並印出回復訊息。

test:

h1 = Hero("亞瑟", 100, 30)
h2 = Hero("哥布林", 50, 10)

h1.attack(h2)
### 輸出：亞瑟 攻擊了 哥布林，造成 30 點傷害！ (哥布林剩餘 HP: 20)
h1.attack(h2)
### 輸出：亞瑟 攻擊了 哥布林，造成 30 點傷害！ (哥布林剩餘 HP: 0)
### 輸出：哥布林 已經陣亡！

---

Ex.2 封裝銀行帳戶與提款限制（封裝 Encapsulation）

題目描述：

設計一個 BankAccount 類別，確保帳戶餘額不能被外部任意修改，並限制提款邏輯。

要求：

初始化函式接收 owner（持有人）與 initial_balance（初始金額），將餘額存入私有屬性 __balance。

方法 deposit(self, amount)：存款金額必須大於 0，成功後印出存款後餘額；若金額小於等於 0 則印出警示。

方法 withdraw(self, amount)：

提款金額必須大於 0。

若提款金額大於目前餘額，印出「餘額不足，提款失敗！」。

扣除成功後印出剩餘金額。

方法 get_balance(self)：回傳目前餘額。

test:

acc = BankAccount("Alice", 1000)
acc.deposit(500)  # 存入 500，餘額: 1500
acc.withdraw(2000)  # 輸出: 餘額不足，提款失敗！
acc.withdraw(800)  # 成功提款 800，餘額: 700
### 直接嘗試 acc.__balance -= 500 會無效或報錯

---

Ex.3 員工與主管薪資計算

題目描述：

公司有一般員工與部門主管，主管除了一般底薪外，還有額外的管理獎金。

要求：

父類別 Employee：

__init__(self, name, base_salary)

方法 calculate_pay(self)：回傳 base_salary。

子類別 Manager（繼承 Employee）：

__init__(self, name, base_salary, bonus)：需使用 super().__init__() 呼叫父類別初始化。

覆寫（Override）calculate_pay(self)：回傳 base_salary + bonus。

兩個類別皆需實作 __str__(self) 方法，印出員工姓名與計算後的總薪資。

test:

emp = Employee("Bob", 40000)
mgr = Manager("Alice", 60000, 20000)

print(emp)  # 輸出: 員工: Bob | 實領薪資: 40000 元
print(mgr)  # 輸出: 主管: Alice | 實領薪資: 80000 元

---

Ex.4 電商多重付款管道
題目描述：
設計一個支援多種結帳方式（信用卡、LINE Pay、貨到付款）的購物系統。

要求：

設計三個結帳類別，皆包含 pay(self, amount) 方法：

CreditCardPayment(card_number)：印出「使用信用卡 {卡號後四碼} 成功扣款 {amount} 元」。

LinePayPayment(phone_number)：印出「已發送 LINE Pay 付款請求至 {電話}，金額 {amount} 元」。

CashOnDelivery()：印出「已建立貨到付款訂單，送達時需收取 {amount} 元」。

設計一個函式 process_checkout(payment_method, total_amount)：

利用多型特性，直接呼叫傳入付款物件的 .pay(total_amount)，不需要使用 if-elif 檢查型態。

test:

p1 = CreditCardPayment("1234-5678-9012-3456")
p2 = LinePayPayment("0912-345-678")
p3 = CashOnDelivery()

process_checkout(p1, 1500)
process_checkout(p2, 350)
process_checkout(p3, 2000)

---

綜合實戰題：智慧會員商城訂單結帳與積分回饋系統

某電商系統接收到一串前端傳入的「非結構化字串訂單」，包含使用者資訊、會員等級與購買清單。因為格式混亂且部分資料破損，系統需要透過文字清洗、類別物件建模與多型折扣計算，最終產出格式化帳單與會員積分報表。

test:

raw_data = """
User: Alice_101 | Tier: VIP | Items: Apple:2, 100.0 ; Banana:5, '30' ; Noise:xxx, 200 ; Cherry:1, 50.0
User: Bob_202 | Tier: Normal | Items: Book:1, 450 ; Pen:'3', 25.0 ; InvalidItem:error, error
User: Charlie_303 | Tier: VVIP | Items: Laptop:1, 30000 ; Mouse:2, '600'
"""
1. 資料清洗與解析（字串、re、字典、例外處理）編寫一個解析函式，逐行解析每一筆訂單：提取 User、Tier 與 Items。Items 之間以分號 ; 分隔，商品與數量以冒號 : 分隔，單價以逗號 , 分隔。數量與單價若被單引號 ' 包覆需去除；若數值無法轉換（如 xxx 或 error），請使用 try-except 忽略該筆異常商品。將清洗乾淨的商品建立為 Item 物件。

2. 商品物件與購物車設計（Class、__str__）設計 Item 類別：屬性：name, quantity (int/float), price (float)。方法 get_subtotal()：回傳該商品小計（$\text{數量} \times \text{單價}$）。魔術方法 __str__()：回傳 "{name} x {quantity} = {小計}元"。

3. 會員階級與多型結帳系統（Inheritance、super()、Polymorphism、封裝）父類別 Member（一般會員）：初始化屬性：user_id, name, items (存放 Item 物件的清單)。私有屬性：__points（初始積分為 0），提供 get_points() 方法讀取。方法 get_raw_total()：計算未打折前的商品總額。多型方法 calculate_final_total()：一般會員無折扣，回傳原始總額。多型方法 add_reward_points(amount)：消費每滿 100 元累積 1 點積分（不滿 100 元無條件捨去）。魔術方法 __str__()：印出會員資訊與商品明細。子類別 VIPMember（繼承 Member）：覆寫 calculate_final_total()：享有 9 折（0.9） 優惠。覆寫 add_reward_points(amount)：消費每滿 100 元累積 2 點 積分。子類別 VVIPMember（繼承 VIPMember）：覆寫 calculate_final_total()：享有 8 折（0.8） 優惠，且若折後金額超過 10000 元，再折 1000 元。覆寫 add_reward_points(amount)：消費每滿 100 元累積 5 點 積分。

4. 系統結帳與排行榜輸出（字典統計與 Lambda 排序）設計結帳流程函式 process_all_orders(members)：遍歷所有會員物件，呼叫多型方法計算結帳金額並發放積分。建立一個統計字典，收集所有會員結帳後的數據。根據「最終結帳金額」由高到低（降冪）排序印出所有會員的帳單明細與獲得積分。


============== 系統結帳報表 ===============

【會員: Charlie_303 (階級: VVIPMember)】
商品明細:
  - Laptop x 1 = 30000.0元
  - Mouse x 2 = 1200.0元
原始總額: 31200.0 元
實付金額: 23960.0 元 (享有 VVIP 特殊折扣)
獲得積分: 1195 點

【會員: Bob_202 (階級: Member)】
商品明細:
  - Book x 1 = 450.0元
  - Pen x 3 = 75.0元
原始總額: 525.0 元
實付金額: 525.0 元 (無折扣)
獲得積分: 5 點

【會員: Alice_101 (階級: VIPMember)】
商品明細:
  - Apple x 2 = 200.0元
  - Banana x 5 = 150.0元
  - Cherry x 1 = 50.0元
原始總額: 400.0 元
實付金額: 360.0 元 (享有 VIP 9折)
獲得積分: 6 點
======================================================