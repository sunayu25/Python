Ex.1 找出第一個能被 7 整除的數

題目描述：
請寫一個程式，利用 for 迴圈與 range() 掃描 1 到 100 之間的所有整數。當程式由小到大找到第一個能被 7 整除的數字時，請立刻印出該數字，並使用 break 結束迴圈（不要繼續往後印）。

---

Ex.2 數字過濾加總

題目描述：
請寫一個程式，利用 for 迴圈計算 1 到 50 之間，所有「是 3 的倍數，但不是 5 的倍數」的數字總和。

---

Ex.3 名單特殊搜尋

題目描述：
給定一個名單串列 names = ["Alice", "Bob", "Charlie", "David", "Eve"]。請用 for 迴圈依序檢查裡面的名字，一旦發現名字的長度大於 6（提示：len(name) > 6），就印出該名字並立刻 break 結束程式。

---

Ex.4 猜數字基礎版

題目描述：
假設正確答案是數字 42。請寫一個程式，利用 for 迴圈限制使用者最多只能猜 3 次。每次猜完，用 if-elif 判斷猜太大、猜太小還是猜中。如果猜中，印出讚美的話並立刻 break；若 3 次都沒猜中，迴圈結束後印出失敗。

---

Ex.5 棋盤格陰陽海

題目描述：請利用兩層 for 迴圈產生一個 3*3 的符號矩陣。
要求：外層代表橫列（列號 1~3），內層代表縱欄（欄號 1~3）。在內層迴圈中，利用 if 判斷「列號加欄號」的和：如果和是偶數印出 O，是奇數印出 X。

O X O 

X O X 

O X O

---

Ex.6 無窮帳號密碼登入驗證

題目描述：
請利用 while True: 建立一個無窮迴圈，在迴圈內要求使用者依序輸入帳號與密碼。只有當帳號輸入 admin 且密碼輸入 1234 時，程式才會印出 登入成功 並透過 break 結束迴圈；否則就會印出 帳密錯誤，請重新輸入 並讓迴圈持續執行。

---

Ex.7 數字累加器與安全停機裝置

題目描述：
請寫一個程式讓使用者不斷輸入整數並進行累加。但為了防止系統過載，請設定兩道結束迴圈（break）的防線：

當使用者輸入 0 時，代表主動結束。

當累加的總和超過 100 時，觸發安全機制強制結束。
結束後，請印出最終的總和。

---

Ex.8 ATM 提款機模擬

題目描述：
假設帳戶初始餘額有 1000 元。請用 while True: 讓使用者不斷輸入提款金額：

如果輸入金額小於或等於 0，印出 輸入錯誤 讓使用者重新輸入。

如果提款金額大於目前餘額，印出 餘額不足 讓使用者重新輸入。

如果金額正常，則扣除餘額。當餘額剛好歸零時，印出 錢包空了 並 break 結束程式。

---

