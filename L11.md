# Lesson 11 物件(Object)、類別(Class)

## ❓Section 1. 什麼是物件(Object)與類別(Class

### 🚗物件(Object)

物件可以想像成一個人，每個人都有自己的屬性，例如：年齡、性別、身高，所以如果我們要使用一個人的物件，我們可以像這樣使用它：

```python
print(People.age)
print(People.height)
```

### ⏱️類別(Class)

類別就是定義一個物件需要有什麼樣的屬性或是函式，要創建一個類別我們可以用以下操作：

```python
class Person: #定義Person 類別
	def __init__(self, age, height): #初始化函式
		self.age = age
		self.height = height

	def birthday(self): #其他自訂函式
		self.age += 1
```

(1) Class 類別名稱 : 類別關鍵字

類別名稱用來標註該類別的種類，通常自訂類別使用大寫作為開頭，例如：Person

(2) init(self): 初始化函式

初始化函式用來定義該類別的初始屬性，例如：age, height，其中的self代表當下的物件本身，所以可以看成“自己的age“，如果產生物件時，想要使用參數調整該物件屬性，則需要將參數設置於初始化函式

(3)自定義函式

使用自定義函式時，可以將物件本身進行改動，例如改變屬性等，若要使用物件屬性，需要將“self”當作參數，使用self.屬性即可讀取

## 📝Section2. 自訂類別

我們已經學到每一種資料型態就是一種類別，所以當我們自訂一個類別時，我們就是創造了一個資料型態。因此，我們也可以對他進行完全的自訂化，類別定義後，我們即可將參數放入，產生一個物件

```python
class Car:
	def __init__(self, brand, price):
		self.brand = brand
		self.price = price

	def discount(self, discount):
		self.price *= discount

AMG_benz = Car("benz", 3000000)
AMG_benz.discount(0.7)
print(AMG_benz.price) 
```

__str__：定義「物件轉為字串」時的格式，方便除錯與檢視。

```python
class Car:

    def __init__(self, brand, price):
        self.brand = brand
        self.price = price

    def __str__(self):
        return f"{self.brand} 汽車 (售價: {self.price} 元)"


my_car = Car("Benz", 3000000)
print(my_car)  # 輸出: Benz 汽車 (售價: 3000000 元)
```

### 類別屬性 vs. 實例屬性（Class Attribute vs. Instance Attribute）

實例屬性（self.xxx）：每個物件各自獨立（如每台車的廠牌、售價）。  

類別屬性（宣告在類別本體）：所有實例共用（可用來統計總共建立了幾台車）。

```python
class Car:
    total_cars = 0  # 類別屬性：共用計數器

    def __init__(self, brand, price):
        self.brand = brand  # 實例屬性
        self.price = price  # 實例屬性
        Car.total_cars += 1  # 每次產生物件，計數器 +1


c1 = Car("Benz", 3000000)
c2 = Car("BMW", 2800000)
print(Car.total_cars)  # 輸出: 2
```

## 物件導向三大核心特性

A. 繼承（Inheritance）— super()。

```python
class Car:
    total_cars = 0  # 類別屬性：共用計數器

    def __init__(self, brand, price):
        self.brand = brand  # 實例屬性
        self.price = price  # 實例屬性
        Car.total_cars += 1  # 每次產生物件，計數器 +1


c1 = Car("Benz", 3000000)
c2 = Car("BMW", 2800000)
print(Car.total_cars)  # 輸出: 2

class ElectricCar(Car):  # 繼承 Car

    def __init__(self, brand, price, battery_capacity):
        super().__init__(brand, price)  # 呼叫父類別的 __init__
        self.battery_capacity = battery_capacity

    def charge(self):
        print(f"{self.brand} 正在充電中，電池容量為 {self.battery_capacity} kWh。")

my_tesla = ElectricCar("Tesla Model 3", 1700000, 75)

# (B) 呼叫子類別專屬的方法 charge()
my_tesla.charge()
```

B. 封裝與私有屬性（Encapsulation / Private Attributes）
防止外部直接竄改敏感資料（例如避免 car.price = -100）。

使用雙底線 __ 定義私有屬性（Private）。

```python
class BankAccount:

    def __init__(self, owner, balance):
        self.owner = owner
        self.__balance = balance  # 雙底線開頭為私有屬性

    def get_balance(self):
        return self.__balance  # 透過方法安全讀取餘額

    def deposit(self, amount):
        if amount > 0:
            self.__balance += amount  # 透過方法安全修改餘額
            print(f"成功存入 {amount} 元！")
        else:
            print("存款金額必須大於 0！")


# 1. 建立帳戶物件
my_account = BankAccount("Alice", 1000)

# 2. 讀取公開屬性
print("帳戶擁有者:", my_account.owner)  # 輸出: Alice

# 3. 讀取私有屬性（透過 get_balance 方法）
print("目前餘額:", my_account.get_balance())  # 輸出: 1000

# 4. 存入款項
my_account.deposit(500)  # 輸出: 成功存入 500 元！
print("更新後餘額:", my_account.get_balance())  # 輸出: 1500
```
C. 多型（Polymorphism）— 相同介面、不同行為

不同物件擁有同名方法，但有各自的運作邏輯（例如：Cat.speak() 和 Dog.speak()）。

```python
# 1. 定義不同的類別，但都擁有相同名稱的方法：speak()
class Dog:

    def speak(self):
        return "汪汪！"


class Cat:

    def speak(self):
        return "喵喵！"



# 2. 將不同的動物物件放進同一個清單
animals = [Dog(), Cat()]

# 3. 使用多型：用完全相同的呼叫方式 animal.speak()
for animal in animals:
    print(animal.speak())

# 輸出結果：
# 汪汪！
# 喵喵！

```